# function that swaps the postion of 2 vaeriable
def swap(a,b):
    temp = a
    a = b
    b = temp
    print(f"The value of a is {a} and b is {b}") # Print the swapped values of a and b.
   
swap(4,6)